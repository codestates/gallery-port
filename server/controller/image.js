module.exports = {
    getProjectImage: (req, res) => {
        //TODO: req.params.path 에 맞는 이미지를 res.sendfile 로 보낸다.
        res.send('ok')
    },

    getProfileImage: (req, res) => {
        //TODO: req.params.path 에 맞는 이미지를 res.sendfile 로 보낸다.
        res.send('ok')
    }
}