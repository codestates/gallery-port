import React from 'react';

function Landing(pops) {
  return (
    <div
      style={{
        width: '100vw',
        height: '100vh',
        position: 'absolute',
        // top: 0,
        // left: 0,
        zIndex: '100vh',
        // position: 'fixed',
      }}
    >
      hi
    </div>
  );
}

export default Landing;
